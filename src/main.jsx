import React from "react";
import ReactDOM from "react-dom/client";
import GESDashboard from "./components/ges_dashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GESDashboard />
  </React.StrictMode>,
);
